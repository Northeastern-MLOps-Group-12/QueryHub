// Provider options for cloud services
export const providerOptions = ["gcp"];

// Database type options for each cloud provider
export const dbTypeOptions: Record<string, string[]> = {
  gcp: ["postgres"],
};
